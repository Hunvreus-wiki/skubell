# Binaries and build artifacts
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Coverage and profiling output
*.out
coverage/
coverage.*
*.coverprofile
profile.cov
*.prof

# Go workspace files
go.work
go.work.sum

# Dependency directories (uncomment to vendor)
# vendor/

# Local environment overrides
.env
.env.*

# IDE and OS files
.DS_Store
Thumbs.db
.idea/

# VS Code: track shareable project config, ignore personal/workspace state.
# Add another `!.vscode/<file>` line to share more (e.g. launch.json, tasks.json).
.vscode/*
!.vscode/settings.json

# Local-only notes and scratch docs (not part of the repo)
local-notes/
